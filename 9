//برنامه‌ای بنویسید که دو عدد را دریافت کرده و بزرگ‌ترین آن‌ها را چاپ کند
#include <iostream>
using namespace std;
int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    if (a > b)
        printf("%d", a);
    else
        printf("%d", b);

    return 0;
}
